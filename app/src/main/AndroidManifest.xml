<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestiondevisitas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestionDeVisitas">
        <activity android:name=".BuscarClienteActivity"></activity>
        <activity android:name=".DetalleClienteFragment" />
        <activity android:name=".DetalleVisitaActivity" />
        <activity
            android:name=".NavegationActivity"
            android:label="@string/title_activity_navegation"
            android:theme="@style/Theme.GestionDeVisitas.NoActionBar" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>